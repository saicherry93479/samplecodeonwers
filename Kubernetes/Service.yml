apiVersion: v1
kind: Service
metadata:
  name: <NAME>
spec:
  selector:
    app: myapp
    environment: <NAME>
  ports:
    - protocol: TCP
      port: <CONTAINER_PORT>
      targetPort: <CONTAINER_PORT>
